# FYH TODO

> 个人可执行待办清单。状态变化记入 `progress.md`，长期取舍记入 `decisions.md`。

## 使用约定

- `[ ]` 待开始，`[>]` 进行中，`[x]` 已完成，`[-]` 已取消。
- 每项只写一个可验收结果，并标注 `P0`（阻塞/最高优先级）、`P1`（常规）或
  `P2`（有空再做）。
- 开始工作时将一项改为 `[>]`；完成或取消时补日期和验证依据。
- 小改动、普通调试和测试不复制到 `progress.md`；只有阶段性状态变化才记录。
- 涉及架构、接口、工具、实验设计或长期流程的决定，追加 ADR 到 `decisions.md`。

## 当前待办

暂无已承诺的个人待办。新增项目时请使用：

```text
- [ ] P1 任务名称 — 验收标准：……；关联文件/issue：……
```
